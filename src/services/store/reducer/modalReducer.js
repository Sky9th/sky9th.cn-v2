import { createSlice } from '@reduxjs/toolkit'export const modalSlice = createSlice({    name: 'modal',    initialState: {        showLogin: false,        showRegister: false,        showPeople: false,        showAvatar: false,        showForgot: false    },    reducers: {        setVisible: (state, action = {}) => {            let modal = action.payload['modal'];            console.log(modal)            state['show'+ modal.substr(0,1).toUpperCase() + modal.substr(1, modal.length - 1)] = !!action.payload['visible']        }    }})// Action creators are generated for each case reducer functionexport const { setVisible } = modalSlice.actionsexport default modalSlice.reducer