import { createSlice } from '@reduxjs/toolkit'import util from "../../../util/util";export const profileSlice = createSlice({    name: 'profile',    initialState: {        fingerprint: "",        userInfo: {},        sessionKey: ""    },    reducers: {        setFingerprint: (state, action) => {            state.fingerprint = action.payload;        },        setSessionKey: (state, action) => {            state.sessionKey = action.payload;            console.log(action)            util.cookies.set('sessionKey', state.sessionKey)        },        setUserInfo: (state, action) => {            state.userInfo = Object.assign({}, state.userInfo, action.payload)            console.log(action)            console.log(state.userInfo)        },        logout: (state) => {            state.userInfo = {}            state.sessionKey = ""        }    }})// Action creators are generated for each case reducer functionexport const { setFingerprint, setSessionKey, setUserInfo, logout } = profileSlice.actionsexport default profileSlice.reducer